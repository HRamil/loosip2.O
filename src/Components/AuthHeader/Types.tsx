import {ImageProps} from 'react-native';

export interface Props {
  title?: string;
  heading?: string;
  icon?: boolean;
  mainHeading?: string;
  para?: string;
  backArrow?: boolean;
  navigation?: any
  // rightIcon?: any;
}
